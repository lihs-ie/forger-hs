module Forger.ForgerSpec (spec) where

import Test.Hspec

spec :: Spec
spec = do
    describe "Forger" $ do
        it "placeholder: library builds and tests run" $ do
            True `shouldBe` True
